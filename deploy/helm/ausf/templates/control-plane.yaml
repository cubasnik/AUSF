{{- $fullname := include "ausf.fullname" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullname }}-control-plane
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ausf.labels" . | nindent 4 }}
    {{- include "ausf.controlplane.selectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.controlPlane.replicaCount }}
  selector:
    matchLabels:
      {{- include "ausf.controlplane.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ausf.labels" . | nindent 8 }}
        {{- include "ausf.controlplane.selectorLabels" . | nindent 8 }}
      {{- with .Values.controlPlane.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: control-plane
          image: "{{ .Values.controlPlane.image.repository }}:{{ .Values.controlPlane.image.tag }}"
          imagePullPolicy: {{ .Values.controlPlane.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.controlPlane.service.port }}
              protocol: TCP
          env:
            {{- range $key, $val := .Values.controlPlane.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            # UDM address — resolves to mock service when mocks.enabled
            {{- if .Values.mocks.enabled }}
            - name: AUSF_UDM_BASE_URL
              value: "http://{{ $fullname }}-mock-udm.{{ .Values.namespace }}.svc.cluster.local:{{ .Values.mocks.udm.service.port }}"
            - name: AUSF_NNRF_BASE_URL
              value: "http://{{ $fullname }}-mock-nrf.{{ .Values.namespace }}.svc.cluster.local:{{ .Values.mocks.nrf.service.port }}"
            {{- end }}
            {{- if .Values.postgresql.enabled }}
            # Database connection — Bitnami PostgreSQL sub-chart.
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://{{ $fullname }}-postgresql.{{ .Values.namespace }}.svc.cluster.local:5432/{{ .Values.postgresql.auth.database }}"
            - name: SPRING_DATASOURCE_USERNAME
              value: {{ .Values.postgresql.auth.username | quote }}
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.auth.existingSecret | default (printf "%s-postgresql" $fullname) }}
                  key: {{ .Values.postgresql.auth.secretKeys.userPasswordKey }}
            {{- else if .Values.externalDatabase.host }}
            # Database connection — external (bring-your-own) PostgreSQL.
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://{{ .Values.externalDatabase.host }}:{{ .Values.externalDatabase.port }}/{{ .Values.externalDatabase.database }}"
            - name: SPRING_DATASOURCE_USERNAME
              value: {{ .Values.externalDatabase.username | quote }}
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.externalDatabase.existingSecret }}
                  key: {{ .Values.externalDatabase.existingSecretKey }}
            {{- end }}
            {{- if .Values.controlPlane.existingSecret }}
            envFrom:
              - secretRef:
                  name: {{ .Values.controlPlane.existingSecret }}
            {{- end }}
          startupProbe:
            # Allows up to 150 s (15 × 10 s) for Spring Boot JVM startup before
            # liveness and readiness probes take over.
            httpGet:
              path: /actuator/health/liveness
              port: http
            failureThreshold: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 10
            failureThreshold: 2
          resources:
            {{- toYaml .Values.controlPlane.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false  # Spring Boot writes tmp files
            capabilities:
              drop:
                - ALL
      {{- with .Values.controlPlane.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controlPlane.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controlPlane.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-control-plane
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ausf.labels" . | nindent 4 }}
    {{- include "ausf.controlplane.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.controlPlane.service.type }}
  ports:
    - port: {{ .Values.controlPlane.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ausf.controlplane.selectorLabels" . | nindent 4 }}
