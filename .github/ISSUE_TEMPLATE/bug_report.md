---
name: Bug report
about: Report a reproducible defect in AUSF flows, validation, or runtime behavior
title: "[Bug]: "
labels: [bug]
assignees: []
---

## Summary

Briefly describe the defect.

## Affected Area

- Layer: `networking` / `control-plane` / `microservices` / `automation` / `docker-compose`
- Flow: `5G_AKA` / `EAP_AKA_PRIME` / `TLS / HTTPS` / `TTL` / `restart persistence` / `NRF / UDM` / other
- Environment: local / compose / CI

## Expected Behavior

Describe the expected result.

## Actual Behavior

Describe the observed result.

## Reproducer

1. 
2. 
3. 

## Validation Context

- Command or script used:

```text
Paste the exact command or smoke script.
```

- Relevant output:

```text
Paste the key error or failing output.
```

## Impact

- Does it block `validate` CI? yes / no
- Does it break a happy path? yes / no
- Does it break a negative path or regression scenario? yes / no

## Notes

Add logs, screenshots, container names, or related PRs if relevant.
